{
  "name": "lambda-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "xprebuild": "rm -rf functions/$FN_DIR/dist",
    "build": "esbuild functions/$FN_DIR/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=functions/$FN_DIR/dist/index.js",
    "postbuild": "cd functions/$FN_DIR/dist && zip -r index.zip index.js*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/note-soy/lambda-api.git"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/note-soy/lambda-api/issues"
  },
  "homepage": "https://github.com/note-soy/lambda-api#readme",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "esbuild": "^0.18.15"
  }
}
